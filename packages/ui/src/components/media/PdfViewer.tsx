import * as React from "react";
// placeholder; later, wrap react-pdf-viewer via adapters
export const PdfViewer = ({ src }: { src: string }) => (
  <div className="rounded-card border p-2 text-sm text-gray-500">PDF preview placeholder: {src}</div>
);